name: arm-migration
description: Azure podinfo webapp migrated from ARM template
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: azure-typescript
