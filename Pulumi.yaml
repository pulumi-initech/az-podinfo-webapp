name: arm-migration
description: ARM to Pulumi migration for podinfo webapp
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: azure-typescript
